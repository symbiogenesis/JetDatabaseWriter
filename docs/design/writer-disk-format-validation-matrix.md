# Writer Disk-Format Validation Matrix

**Status:** Active cross-feature coverage matrix, reconciled on 2026-05-25 with the earlier matrix shape and the completed validation backlog, with test-name references re-verified against the suite on 2026-06-30. No broad unresolved validation item is currently open.

This document is the compact map of writer-emitted Access disk-format surfaces, their base fixture or oracle, their strongest automated validation signal, and the residual trigger that would justify more coverage. General validation rules live in [dao-validation-strategy.md](dao-validation-strategy.md). Detailed compatibility history lives in [round-trip-openrecordset-hypothesis.md](round-trip-openrecordset-hypothesis.md) and [catalog-index-maintenance-notes.md](catalog-index-maintenance-notes.md).

## Validation Levels

Use the canonical ladder in [dao-validation-strategy.md](dao-validation-strategy.md). In this matrix, the levels are named as follows:

- **Reader round-trip:** writer output is reopened through JetDatabaseWriter's reader and assertions cover schema, data, index metadata, or decoded payloads.
- **DAO OpenRecordset:** a bitness-matched Windows PowerShell host drives `DAO.DBEngine.120` against writer output and materializes recordsets or DAO index seeks.
- **DAO CompactDatabase:** DAO compacts writer output, then JetDatabaseWriter reopens the compacted file and verifies schema, data, relationships, passwords, payloads, or page-reuse invariants.

Manual Microsoft Access UI verification is supplemental evidence. It remains useful for workflows DAO does not expose, such as saving an attachment back to disk through the UI, but it is not stronger than DAO automation when the same scenario can be automated.

## Update Rules

- When a writer disk-format feature changes, add or update the strongest feasible validation signal and refresh the relevant row below.
- For every DAO CompactDatabase test, reopen the compacted output and assert the feature that motivated the test.
- Prefer Access-authored fixtures such as [NorthwindTraders.accdb](../../JetDatabaseWriter.Tests/Databases/NorthwindTraders.accdb), DAO-authored scratch databases, or Access/DAO-authored payload bytes as the oracle side of DAO/Access compatibility tests. Writer-created fresh databases are subjects under test, not fixture oracles; use them only when validating bootstrap or generated writer output itself.
- Promote any new DAO baseline probe invariant into a focused regression test.
- If a residual gap is accepted, name the trigger that would make it release-relevant instead of leaving a standing backlog item.

When the matrix says "writer-created", read that as the output under validation. Do not infer that a writer-created database is an engine-authored source of truth.

## Coverage Matrix

| Area | Base fixture / oracle | Strongest automated signal | Current coverage and tests / probes | Residual trigger |
|---|---|---|---|---|
| Fresh Jet4/ACE bootstrap | Writer-created ACCDB bootstrap output under validation; not a comparison oracle for unrelated Access behavior. | DAO CompactDatabase | Fresh writer-created ACCDB streams scaffold hidden core system tables, `MSysRelationships`, `MSysComplexColumns`, complex-type attachment template metadata, a simple user table, and inserted rows; the compacted copy reopens through this reader. Current signal: `FreshWriterCreatedDatabase_DaoCompactSmokeTest` in [DaoStorageMaintenanceTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/DaoStorageMaintenanceTests.cs), plus [CreateDatabaseTests.cs](../../JetDatabaseWriter.Tests/Writer/CreateDatabaseTests.cs) and [ComplexColumnsWriterTests.cs](../../JetDatabaseWriter.Tests/ComplexColumns/ComplexColumnsWriterTests.cs). | Add byte-for-byte comparison with a DAO-created empty database only if empty-database parity becomes a release requirement. |
| Scalar user tables and LVAL values | DAO-authored ACCDB host mutated by the writer, plus DAO-authored memo payloads as reader oracles. | DAO OpenRecordset | DAO can count writer-mutated rows, seek primary keys, continue AutoNumber values, and round-trip MEMO embedded NUL/CJK text plus deterministic OLE binary payloads large enough to force LVAL storage. Current signal: `DaoOpenRecordset_RowCount_MatchesWriterOutput`, `DaoIndexTraversal_Seek_LocatesRowByPrimaryKey`, `DaoAutoNumber_Continuation_NextIdFollowsLastWriterInsert`, and `DaoMemoAndOleLvalFidelity_EmbeddedNulsCjkAndBinary_RoundTripExactly` in [DaoValidationTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/DaoValidationTests.cs). | Automate Access UI save/export workflows only if those UI paths become release blockers. |
| PK/FK catalog and TDEF metadata | Access-authored Northwind copy mutated by the writer. | DAO CompactDatabase | Single-column and composite PK/FK metadata survives DAO compact; FK enforcement rejects invalid child rows; wide endpoint rename preserves both ends; relationship drop reclaims shortened TDEF chains; Type 8 relationship `MSysObjects` rows are emitted on create and left for CompactDatabase to normalize on mutation. Current signals: `SinglePk_AndSingleColumnFk_SurviveCompactAndRepair` and `CompositePk_AndMultiColumnFk_SurviveCompactAndRepair` in [AccessRoundTripTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/AccessRoundTripTests.cs); `DaoRelationshipEnforcement_FkViolation_RaisesError` in [DaoValidationTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/DaoValidationTests.cs); `IndexRebuildAndShortenedTDefChain_SurviveCompactAndRepair` and `RelationshipRenameOnMultiPageTDef_SurvivesCompactAndRepair` in [DaoStorageMaintenanceTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/DaoStorageMaintenanceTests.cs). | Add broader rename/drop permutations or Access-authored byte comparisons only when a relationship mutation changes TDEF logical-index or `MSysRelationships` bytes. |
| Multi-table index/FK/allocation stress | Access-authored Northwind copy mutated by the writer. | DAO CompactDatabase | Multi-table writer output survives DAO compact with table survival, all stress child row counts, generated FK index names, and matching `MSysRelationships` rows intact. Current signal: `DaoCompact_MultiTableStress_SurvivesCompactAndRepair` in [DaoValidationTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/DaoValidationTests.cs). | Add byte-for-byte Access index rebuild equivalence only if allocator or index rebuild shape changes enough to make parity release-relevant. |
| Storage maintenance and remanence | Access-authored Northwind copy mutated by the writer, plus byte-level marker checks on generated files. | DAO CompactDatabase plus byte-level regression checks | Page free-list reuse, tail-only shrink behavior, secure erase of deleted row bodies and old LVAL chains, ordinary user-table replaced index pages, and shortened TDEF-chain reclamation are pinned without promising full Access-style live-page compaction, live-page slack scrubbing, or broader orphan-page reclamation beyond those pinned cases. Current signals: [DaoStorageMaintenanceTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/DaoStorageMaintenanceTests.cs), [PageAllocatorTests.cs](../../JetDatabaseWriter.Tests/Pages/PageAllocatorTests.cs), and [DataRemanenceTests.cs](../../JetDatabaseWriter.Tests/Writer/DataRemanenceTests.cs). | Design separately before enabling full Access-style live-page compaction/renumbering, arbitrary unused-gap scrubbing, broader orphan-page reclamation, or replacement-index-page reclamation for `MSys*` and generated complex flat tables. |
| Access-native Agile encryption | Access-authored Northwind copy mutated and encrypted by the writer, plus encryption fixtures. | DAO CompactDatabase | Flat Access-native Agile ACCDB output survives DAO compact and reopens with password handling and encrypted table data intact. CFB Agile remains a separate Office Crypto compatibility path, not an Access-native DAO compact target. Current signals: `DaoCompactDatabase_OnEncryptedOutput_ReopenSucceeds` in [DaoValidationTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/DaoValidationTests.cs), `EncryptDecrypt_AccdbAgile_RoundTripsThroughChangePassword` and `EncryptDecrypt_AccdbAgileCfb_RoundTripsThroughChangePassword` in [EncryptionMutationTests.cs](../../JetDatabaseWriter.Tests/Encryption/EncryptionMutationTests.cs), and Office Crypto Agile CFB fixture coverage in [AgileEncryptionTests.cs](../../JetDatabaseWriter.Tests/Encryption/AgileEncryptionTests.cs). | Extend when a new encryption format or password-mutation path is added. |
| Complex columns on Access-authored hosts | Access-authored Northwind copy mutated by the writer. | DAO CompactDatabase | Northwind-hosted writer-created attachment and multi-value complex columns preserve wrapper-encoded `FileData`, Access-style chained-LVAL payloads, flat-table index maintenance, and add-then-rename schema evolution through DAO compact. Current signal: `DaoCompact_ComplexColumnsWithLvalPayload_SurviveCompactAndRepair` in [DaoValidationTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/DaoValidationTests.cs). | Add coverage when a new complex-column mutation becomes release-critical and can be hosted on an Access-authored fixture. |
| Writer-authored complex rows and complex catalog (`ComplexFields` host) | Access-authored `ComplexFields` complex fixture mutated by the writer for the DAO-compact signal; fresh writer-created complex streams as subjects under test for the reader-side suite. | DAO CompactDatabase | Writer-authored attachment rows on the `ComplexFields` host — multiple parent rows, multiple LVAL-backed attachments on one parent, and an LVAL-backed attachment on another parent — survive DAO compact, and the compacted complex catalog (hidden `MSys*` tables, `MSysComplexColumns`, attachment-template columns) reopens through this reader. Fresh writer-created complex coverage — grouped multi-value rows, decoded attachment bytes, row APIs, and flat-table index metadata — is validated reader-side. Current signals: `ComplexFixture_WriterAttachmentRowsSurviveCompactAndRepair` and `ComplexFixtureCatalog_SurvivesCompactAndRepair` in [DaoStorageMaintenanceTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/DaoStorageMaintenanceTests.cs), plus [ComplexColumnsRowApiTests.cs](../../JetDatabaseWriter.Tests/ComplexColumns/ComplexColumnsRowApiTests.cs), [ComplexColumnsFlatIndexesTests.cs](../../JetDatabaseWriter.Tests/ComplexColumns/ComplexColumnsFlatIndexesTests.cs), [ComplexColumnsSchemaEvolutionTests.cs](../../JetDatabaseWriter.Tests/ComplexColumns/ComplexColumnsSchemaEvolutionTests.cs), and the broader `ComplexColumns*Tests` suite. | Extend when bootstrap, complex-template scaffolding, complex row APIs, schema evolution, or complex flat-table index metadata changes. The Northwind-hosted row remains the Access-authored-host signal for schema evolution and chained-LVAL payloads. |
| Linked Access-file Type 6 tables | Writer-created host database linking an Access source, compared against DAO-authored linked-row fixtures. | DAO CompactDatabase plus compacted OpenRecordset | Writer-created Access-file links allocate catalog-only negative ids, avoid low-24 collisions, use fixture-aligned flags, stamp linked-object ACE rows, leave `Lv` / `LvProp` / `LvModule` / `LvExtra` null like DAO-authored Access links, preserve `Database` through DAO compact with DAO-compatible uncompressed UCS-2 LVAL storage, and pass DAO OpenRecordset on the compacted linked table. Current signals: `CreateLinkedTableAsync_AccessSourceLeavesDaoCacheColumnsNull` and `CreateLinkedTableAsync_AccessSource_DaoCompactAndOpenRecordsetSucceed` in [LinkedTableCatalogWriterTests.cs](../../JetDatabaseWriter.Tests/Relationships/LinkedTableCatalogWriterTests.cs). | Extend when linked metadata routing, cache-column semantics, `Database` LVAL storage, or linked-source read-through rules change. |
| Linked text/CSV Type 6 tables | Writer-created host database linking a text source, compared against DAO-authored text-link shape. | DAO CompactDatabase plus compacted OpenRecordset | Writer-created text links use text-link flags, encode the text source filename with Access's `#` separator in raw `MSysObjects.ForeignName`, decode that name back to normal filenames for `ListLinkedTablesAsync`, leave `Lv` / `LvProp` / `LvModule` / `LvExtra` null, preserve `Database` through DAO compact via the DAO-shaped uncompressed UCS-2 LVAL path, and pass compacted OpenRecordset. Current signals: `CreateLinkedTextTableAsync_AllocatesCatalogId` and `CreateLinkedTextTableAsync_TextSource_DaoCompactAndOpenRecordsetSucceed` in [LinkedTableCatalogWriterTests.cs](../../JetDatabaseWriter.Tests/Relationships/LinkedTableCatalogWriterTests.cs). | Extend when text-link flags, raw filename encoding, `Database` LVAL storage, or linked-source read-through behavior changes. |
| Linked ODBC Type 4 cached-schema tables | Writer-created ODBC link rows using caller-supplied Access/DAO cached-schema `LvProp` payloads. | DAO CompactDatabase | The cached-schema overload validates a non-placeholder `MR2\0` / `KKD\0` property block, stores the caller-supplied Access/DAO cached linked-schema payload verbatim, and survives DAO CompactDatabase. DAO OpenRecordset remains DSN/source-dependent. Current signals: `CreateLinkedOdbcTableAsync_CachedSchemaLvPropStoresRealPayload`, `CreateLinkedOdbcTableAsync_CachedSchemaLvPropRejectsPlaceholder`, and `CreateLinkedOdbcTableAsync_CachedSchemaLvProp_DaoCompactSucceeds` in [LinkedTableCatalogWriterTests.cs](../../JetDatabaseWriter.Tests/Relationships/LinkedTableCatalogWriterTests.cs). | Add source-backed OpenRecordset coverage only when a reliable DSN/source fixture exists, or when the cached-schema contract changes. |
| Linked-table fixture oracles and ODBC `LvProp` probe | Access-authored linked Access/ODBC/text fixture rows, Jackcess, and mdbtools. | FormatProbe / fixture regression | Access-authored linked Access/ODBC fixture rows carry non-placeholder `MSysObjects.LvProp` payloads, pinning the cached-schema signal used by the ODBC cached-schema overload. DAO-authored Access-file and text Type 6 links are pinned as allowing null `Lv` / `LvProp` / `LvModule` / `LvExtra`. The `linked-odbc-lvprop` probe dumps the Access-authored ODBC fixture cache, the Type 6 comparison fixture, `NameMap` string runs, target/property frequencies, and parser/builder structural parity. Current signals: [LinkedOdbcLvPropProbe.cs](../../JetDatabaseWriter.FormatProbe/LinkedTables/LinkedOdbcLvPropProbe.cs) and [format-probe-linked-odbc-lvprop.md](../format-probe/format-probe-linked-odbc-lvprop.md). | Promote new probe findings into focused regression tests if they identify a root-cause invariant. Public ODBC link generation is covered in the next row. |
| Generated linked ODBC Type 4 `LvProp` tables | Writer-created ODBC link rows with generated metadata. | Reader/regression coverage plus DAO CompactDatabase where source-independent | Public ODBC link creation writes parseable table-level `MR2\0` / `KKD\0` property blocks with generated `NameMap` data instead of the legacy 12-byte placeholder. The source-column overload emits per-column targets, linked-cache `GUID` binary properties, and a `NameMap` containing remote table/column identity strings. Current signals: `CreateLinkedOdbcTableAsync_MetadataOnly_GeneratesRealTableLvProp`, `CreateLinkedOdbcTableAsync_SourceColumnsGeneratesRealSchemaLvProp`, and `CreateLinkedOdbcTableAsync_SourceColumnsRejectsEmptySchema` in [LinkedTableCatalogWriterTests.cs](../../JetDatabaseWriter.Tests/Relationships/LinkedTableCatalogWriterTests.cs). | Extend when a new public ODBC link mode needs richer schema-cache synthesis or source-backed OpenRecordset validation. |
| Jet3 index emission and maintenance | Access-authored Jet3 `.mdb` fixture mutated by the writer, writer-created Jet3 streams, and Access-authored read fixtures. | Conditional DAO CompactDatabase | Jet3 reader/writer coverage includes single-column text/int indexes, unique mixed-direction composite indexes, update/delete/insert maintenance, and conditional DAO compact on hosts that can open Access 97 `.mdb` files. The DAO test mutates an Access-authored Jet3 host with a writer-created indexed table, primary and normal single-column indexes, post-insert update/delete/insert B-tree maintenance, DAO pre-compact OpenRecordset, and reader verification of the compacted copy. Current signals: `Jet3IndexEmissionAndMaintenance_SurviveCompactAndRepair` in [DaoStorageMaintenanceTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/DaoStorageMaintenanceTests.cs), `MixedDirectionUniqueCompositeIndex_UpdateDeleteInsert_RoundTrips` in [IndexMaintenanceTests.cs](../../JetDatabaseWriter.Tests/Indexes/IndexMaintenanceTests.cs), Jet3 parameterizations in [IndexWriterTests.cs](../../JetDatabaseWriter.Tests/Indexes/IndexWriterTests.cs), [IndexMaintenanceTests.cs](../../JetDatabaseWriter.Tests/Indexes/IndexMaintenanceTests.cs), [IndexIncrementalMaintenanceTests.cs](../../JetDatabaseWriter.Tests/Indexes/IndexIncrementalMaintenanceTests.cs), [IndexBulkInsertStressTests.cs](../../JetDatabaseWriter.Tests/Indexes/IndexBulkInsertStressTests.cs), and [format-probe-appendix-jet3-index.md](../format-probe/format-probe-appendix-jet3-index.md). | Modern Access/DAO 120 can reject Jet3 with "Cannot open a database created with a previous version of your application"; on those hosts the test skips and reader round-trip remains the strongest local signal. Add Access-authored byte comparison only when a reliable Jet3 DAO host exists and exact byte parity becomes release-relevant. |
| Advanced ACE/Jet4 index encodings and B-tree maintenance | DAO-authored ACCDB host mutated by the writer, writer-created outputs as subjects under test, and Access-authored read fixtures. | DAO CompactDatabase | DAO seeks writer-created mixed-direction composite and GUID indexes before compaction, then compacts and reopens to assert GUID, descriptor-scale NUMERIC, BINARY, mixed-direction metadata, descending col-map flag preservation, and post-update/delete/insert row state. Current signals: `AdvancedIndexKeysAndBTreeMaintenance_SurviveCompactAndRepair` in [DaoStorageMaintenanceTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/DaoStorageMaintenanceTests.cs), index tests under [JetDatabaseWriter.Tests/Indexes](../../JetDatabaseWriter.Tests/Indexes), and appendices under [docs/format-probe](../format-probe). | Add Access-authored byte comparisons for remaining key encodings or individual surgical split/merge sub-phases only when those bytes change. |
| System-table and catalog-index maintenance | Access-authored Northwind / Jet3 fixtures, writer-created catalog growth fixtures, and catalog-specific regression tests. | Reader/regression coverage plus DAO CompactDatabase where applicable | Indexed TDEF zero-slot decode bails instead of silently reporting success; `MSysRelationships` / `MSysACEs` / `MSysComplexColumns` inserts and `MSysACEs` deletes use incremental-or-skip maintenance and throw on incremental bail; Jet4/ACE `MSysObjects` deletes and renames fail fast on incremental failure; Jet3 `MSysObjects` catalog splicing is regression-guarded; Type 4/6 linked rows route through catalog-index splicing; catalog splice failures roll back linked-table rows before ACE insertion; low-level catalog helpers reject duplicate `(ParentId, Name)` rows; supported `MSysObjects` catalog insert split/ancestor cases update in place or rebuild only the affected index tree from existing index entries plus the inserted row pointer. Current signals: [LinkedTableCatalogWriterTests.cs](../../JetDatabaseWriter.Tests/Relationships/LinkedTableCatalogWriterTests.cs), [AccessRoundTripTests.cs](../../JetDatabaseWriter.Tests/RoundTrip/AccessRoundTripTests.cs), [RelationshipWriterTests.cs](../../JetDatabaseWriter.Tests/Relationships/RelationshipWriterTests.cs), and [catalog-index-maintenance-notes.md](catalog-index-maintenance-notes.md). | Extend when a new system-table writer touches indexed catalog rows, or when a splice/rebuild fallback adds a new failure mode. Remaining splice `false` returns should stay fail-fast malformed-input or staging guards. |
| DAO-authored baseline probes | DAO-authored copies retained as diagnostic oracles beside writer-mutated Access-authored fixtures. | DAO OpenRecordset / DAO CompactDatabase plus focused byte-level regression guards | `rt-dao-baseline`, `fk-dao-baseline`, and related probes remain diagnostic oracles, but root-cause findings are promoted into ordinary regression tests. Historical findings for OpenRecordset, FK enforcement, FK CompactDatabase, Jet4/ACE TDEF magic, real-index descriptor magic, user-table `Owner` / `LvProp`, `MSysACEs.FInheritable`, text-column `AllowZeroLength` / `Required`, and FK logical cross-references are already promoted. Current signals: [DaoBaselineProbe.cs](../../JetDatabaseWriter.FormatProbe/Dao/DaoBaselineProbe.cs), [FkDaoBaselineProbe.cs](../../JetDatabaseWriter.FormatProbe/Dao/FkDaoBaselineProbe.cs), [Jet4FormatCookieTests.cs](../../JetDatabaseWriter.Tests/Pages/Jet4FormatCookieTests.cs), [PersistedColumnPropertiesTests.cs](../../JetDatabaseWriter.Tests/Schema/PersistedColumnPropertiesTests.cs), and [RelationshipWriterTests.cs](../../JetDatabaseWriter.Tests/Relationships/RelationshipWriterTests.cs). | A new probe finding should become a focused regression test instead of remaining only in probe output. |

## Current Scope Decisions

- There is no standing cross-feature validation backlog in this document.
- The former ESE-inspired coverage review is closed. ESE remains useful only as
	a reference checklist for analogous storage-engine risk categories: page
	mutation invariants, structural validation, delete/replace scrubbing,
	transaction durability, cache behavior, and long-value cleanup. Local
	coverage for those themes now lives in the matrix rows above, especially
	storage maintenance/remanence, complex columns/LVAL, public index seek,
	CompactDatabase automation, transaction tests, emitted-page invariant tests,
	and cache eviction tests. Future ESE-derived concerns should become matrix
	rows or feature-specific DAO/regression tests, not a separate backlog.
- Full Access-style live-page compaction/renumbering stays out of scope unless the project grows an explicit Compact and Repair feature.
- Byte-for-byte DAO/Access comparisons stay diagnostic unless exact byte parity becomes a release requirement for the specific area.
- ODBC OpenRecordset remains source- and DSN-dependent; current automated coverage validates metadata generation, cached-schema preservation, and CompactDatabase compatibility.
- Manual Access UI workflows remain supplemental for cases DAO cannot expose, such as saving an attachment back to disk through the UI.
